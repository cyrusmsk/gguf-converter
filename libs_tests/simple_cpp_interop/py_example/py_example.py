import example

# Prepare input data (a list of strings)
input_data = ["apple", "banana", "cherry"]

# Call the C++ function
result = example.process_strings(input_data)

# Print the result
print(result)
# Output: ['apple_processed', 'banana_processed', 'cherry_processed'
